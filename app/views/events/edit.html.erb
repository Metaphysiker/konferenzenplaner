<%= simple_form_for @event do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.collection_check_boxes :tag_list, @categories, :name, :name, {:item_wrapper_class => 'checkbox_container'}%><br>
    <%= f.button :submit %>
<% end %>